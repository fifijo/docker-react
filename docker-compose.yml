version: "3.8"

services:
  app:
    image: front-ui
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      - "./app:/app"
      - "./app/node_modules"
    ports:
      - 3000:3000
    stdin_open: true
